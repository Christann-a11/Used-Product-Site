import React from 'react';
import AdsComponent from './AdsComponent';

const HomeComponent = () => {
  return (
    <div>
      <h1>Welcome to ProductSell</h1>   
       
      {/* AdsComponent is now rendered via its own route */}
    </div>
  );
};

export default HomeComponent;